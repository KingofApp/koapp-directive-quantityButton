# Koapp QuantityButton Directive v0.0.1

This directive is used in __koa's__ **Woocommerce** modules for changing the number of products

## Used

```html
<koapp-quantity value="{{item.quantity}}" id="item-{{product.id}}"></koapp-quantity>
```
